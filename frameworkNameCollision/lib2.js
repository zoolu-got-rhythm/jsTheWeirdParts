//var libraryName = "lib 2"; 

window.libraryName = window.libraryName || "lib 2"; 

//this checks the global namespace to see if libraryName already exists. 

//check to see if this var is already appended in the global execution context,
//var (window).